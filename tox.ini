[flake8]
# Migrated from setup.cfg (was the active flake8 config; tox.ini was shadowed).
exclude =
	.tox
	build
	dist
	.eggs
	docs/conf.py
max-line-length = 88
select = C,E,F,W,B,B950
extend-ignore = E203, E501
ignore = E713,W503,F841
